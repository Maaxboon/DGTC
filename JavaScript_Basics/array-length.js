const trainStations = ['Manchester Piccadilly', 'Huddersfield', 'Leeds', 'Hull'];

console.log(trainStations.length);

const lastStop = trainStations[trainStations.length -1];

console.log(lastStop);
