// JavaScript provides a total of six (or strictly speaking seven, see box) different data types

/*primitive data types:
 for representations of numbers (data type number), character strings (data type string), and Boolean truth values (data type boolean).
 Then, we have the special data types null and undefined (also a form of primitive data types) as l as a further data type for objects*/

//  Numbers
const number1 = 5; // definition of an integer
const number2 = 0.5; // definition of a decimal
const number3 = -22; // definition of a negative number
const number4 = -0.9; // definition of a negative decimal

// Strings
const firstName = "Wuod"; // Double quotes
const lastName = "Jadolo"; // Double quotes
const age = "18"; // not a number but a string
// const street = 'Bumbogo Cell"; // Syntax error: mixed form
