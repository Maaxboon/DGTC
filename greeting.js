// Store the users name in a variable
let userName = 'Marcin';

// Create a message using the name
let greeting = "Hello, " + userName + "!";

// Display greeting on the screen
console.log(greeting);
